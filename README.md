# lucastuxnet.github.io

Site pessoal — https://pwned.com.br

Hospedado no GitHub Pages. Para publicar:

```bash
git add index.html CNAME README.md
git commit -m "Novo site"
git push origin main
```

Em Settings > Pages, defina Source = branch `main` / root e Custom domain = pwned.com.br.
